<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Document</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">

    <!-- Logout -->
    <div class="w-full flex justify-end">
      <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">Logout</a>
    </div>



    <h5 class="mb-5">edit your post.</h5>

    <!-- Post Form -->
    <form action="/update/<%= post._id %>" method="POST">
      <textarea name="content" placeholder="What's on your mind?" class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %> </textarea>
      <input class="w-40 px-3 py-2 text-sm bg-yellow-500 text-black block rounded-md mt-2" type="submit" value="Update your post">
    </form>

   
  </div>
</body>
</html>
